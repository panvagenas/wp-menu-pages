<h1>
    {{ page.title }}
    {% if page.subtitle %}
        <small>{{ page.subtitle }}</small>
    {% endif %}

    <div class="pull-right">
        <div class="col-md-12">
            <a class="btn btn-md btn-warning btn-export-options" href="">
                <i class="fa fa-download"></i>
            </a>
            <a class="btn btn-md btn-primary btn-import-options" href="">
                <i class="fa fa-upload"></i>
            </a>
        </div>
    </div>
</h1>